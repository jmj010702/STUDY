## 근태 출근 테이블을 일반 테이블에서 파티션 테이블로 재설계

> ⚠️ **이 문서에는 측정치가 없다.** 당시 기록을 유실해서 파티션 키와 전후 성능 비교를 복원할 수 없다.
> 남아 있는 것은 **왜 그렇게 판단했는가**뿐이고, 이 문서는 그 판단을 남기는 것이 목적이다.

### 문제 배경

HR 시스템의 **근태 출근 기록 테이블**을 처음에 일반 테이블로 설계했다.
부트캠프 프로젝트 규모(테스트 데이터 수백~수천 건)에서는 아무 문제가 없었다.

### 문제 — 현직자 멘토의 지적

멘토링에서 **실무 규모를 가정하면 버티지 못한다**는 지적을 받았다.

```
회사당 임직원 3만 명 × 도입사 50개
        ↓
출근 기록은 1인당 1일 1건 이상 계속 쌓인다
        ↓
조회 쿼리가 테이블 전체를 훑는 순간 감당이 안 된다
```

**출근 기록은 "계속 쌓이기만 하고 지워지지 않는" 성격의 데이터**라는 게 핵심이다.
주문·결제처럼 건수가 트래픽에 비례하는 게 아니라, **직원 수 × 날짜 수로 결정론적으로 증가**한다.
즉 **언제 커질지 예측 가능한데, 예측하지 않고 설계했다.**

### 시도 방법 — 두 대안을 검토했다

| 대안 | 내용 | 판단 |
|---|---|---|
| **쿼리 튜닝으로 대응** | 인덱스 추가, 조회 조건 최적화 | **기각** |
| **테이블 구조 자체를 변경** | 파티션 테이블로 재설계 | **채택** |

**쿼리 튜닝을 기각한 이유**

인덱스는 **"많은 행 중에서 몇 건을 빨리 찾는" 문제**를 푼다.
그런데 근태 조회는 대체로 **"특정 기간의 많은 행을 다 가져오는"** 형태다
(월별 근태 집계, 연차 정산, 부서별 출근 현황).

이 경우 인덱스를 타도 결국 그 기간의 행을 다 읽어야 하고,
**테이블이 커질수록 읽어야 할 물리적 범위가 계속 넓어진다.**
튜닝은 상수를 줄이지만 **증가 추세 자체를 바꾸지 못한다.**

파티셔닝은 **읽어야 할 범위 자체를 잘라낸다**(파티션 프루닝).
"이번 달 데이터"를 조회하면 다른 달 파티션은 아예 안 본다.

### 해결 — 파티션 테이블로 재설계

얻은 것 두 가지:

| | 내용 |
|---|---|
| **조회 성능** | 파티션 프루닝으로 스캔 범위 축소 |
| **데이터 보관 안정성** | 오래된 파티션 단위로 아카이빙·삭제 가능. `DELETE`로 수천만 행을 지우는 것과 파티션을 `DROP`하는 것은 비용이 다르다 |

### ⚠️ 복원하지 못한 것

| 항목 | 상태 |
|---|---|
| **파티션 키** | 미확인 — 날짜(월별)인지 회사 ID인지 기록이 없다 |
| **파티션 전략** | RANGE / LIST / HASH 중 무엇이었는지 미확인 |
| **전후 성능 비교** | **측정하지 않았거나, 측정했어도 기록 유실** |

**파티션 키가 무엇이었는지가 이 결정의 절반이다.**
날짜 키면 "기간 조회"에 최적이지만 특정 회사만 보는 쿼리에는 도움이 안 되고,
회사 ID 키면 반대다. 멀티테넌트 + 기간 조회가 둘 다 있으면 복합 파티셔닝을 검토해야 한다.
**이걸 못 적어둔 게 이 문서의 가장 큰 구멍이다.**

### 배운 점

**1. 실무 규모를 가정하고 설계했는가**

부트캠프 프로젝트가 좀처럼 갖지 못하는 관점이다.
"동작한다"와 "3만 명 × 50개사에서도 동작한다"는 다른 문제이고,
**후자는 코드가 아니라 설계 단계에서 결정된다.**

**2. 손쉬운 대안을 검토하고 기각한 과정이 결론보다 중요하다**

"파티셔닝을 적용했다"만 남기면 왜 그게 필요했는지가 사라진다.
**쿼리 튜닝으로 안 되는 이유**를 설명할 수 있어야 그 결정이 내 것이 된다.

**3. 🔴 수치는 그 자리에서 적는다**

이 문서에 측정치가 없는 이유가 그거다.
**"나중에 정리해야지"는 기록을 남기지 않는다는 뜻이다.**
설계를 바꿀 때는 **바꾸기 전에 한 번, 바꾸고 나서 한 번** 재고 그 자리에서 적을 것.
