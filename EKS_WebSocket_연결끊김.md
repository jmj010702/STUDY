## EKS에서 WebSocket 연결이 30초~1분마다 끊긴다

> ⚠️ **이 문서에는 측정치와 설정값이 없다.** 당시 기록을 유실했다.
> 증상과 해결 방향만 남기고, **다음에 같은 증상을 만났을 때의 점검 순서**를 정리해둔다.

관련: [EKS_ingress.md](EKS_ingress.md) — 같은 환경에서 WebSocket/SSE를 어느 파드에 붙일 것인가 문제

### 문제 배경

Pocha_On 프로젝트에서 실시간 기능(WebSocket)을 EKS 환경에 배포했다.
**로컬에서는 멀쩡했다.**

### 문제

배포 환경에서만 **WebSocket 연결이 30초~1분마다 끊겼다.**

- 애플리케이션 에러는 아니었다 — 로직이 실패한 게 아니라 **연결 자체가 사라졌다**
- **주기적**이라는 게 핵심 단서였다. 랜덤이 아니라 일정 시간마다 끊긴다는 건
  **누군가 타이머를 들고 끊고 있다**는 뜻이다

### 원인 — 유휴 연결 타임아웃

WebSocket은 **연결을 열어두고 데이터가 없으면 조용히 있는다.**
그런데 중간 경로의 프록시·로드밸런서는 **"아무것도 안 흐르는 연결"을 죽은 것으로 보고 끊는다.**

```
클라이언트 ─── LB ─── Ingress(nginx) ─── 파드
                ↑          ↑
        각 구간마다 유휴 타임아웃이 따로 있다
```

**HTTP 요청/응답은 짧게 끝나므로 이 타임아웃에 안 걸린다.**
WebSocket만 걸린다 — 그래서 로컬(중간 경로 없음)에서는 재현이 안 됐다.

### 해결

**서버 쪽 연결 유지 시간을 늘려서 해결했다.**

⚠️ **어느 계층에서 몇 초로 바꿨는지는 기록이 유실됐다.**

### 다음에 같은 증상을 만나면 — 점검 순서

> 아래는 당시 확인한 것이 아니라, **이 증상의 원인이 될 수 있는 지점 목록**이다. 미검증.

**① 끊기는 주기를 먼저 잰다**

정확히 60초인지 30초인지가 **어느 계층인지를 알려준다.**
각 구성요소의 기본 타임아웃 값이 다르기 때문에, 주기가 곧 범인의 지문이다.

**② 계층을 하나씩 확인한다**

| 계층 | 볼 것 |
|---|---|
| AWS LB (ALB/NLB) | idle timeout 설정 (ALB 기본 60초) |
| Ingress (nginx) | `proxy-read-timeout` / `proxy-send-timeout` |
| 애플리케이션 | WebSocket 세션 타임아웃, STOMP heartbeat 설정 |

**③ 타임아웃을 늘릴 것인가, 하트비트를 보낼 것인가**

| 방법 | 성격 |
|---|---|
| 타임아웃 늘리기 | 간단하지만 **계층마다 다 늘려야** 하고, 진짜 죽은 연결도 오래 붙잡는다 |
| **주기적 ping/heartbeat** | 연결에 트래픽을 흘려 "살아있음"을 알린다. 근본적이지만 클라이언트·서버 양쪽 구현 필요 |

**둘 중 하나가 아니라 보통 둘 다 쓴다** — 타임아웃보다 짧은 주기로 하트비트를 보내는 구성.

### 배운 점

> **"로컬에서는 되는데 배포하면 안 된다"는 대개 중간 경로의 문제다.**
> 로컬에는 LB도 Ingress도 없다. 그 사이에 낀 것들이 각자의 규칙으로 연결을 다룬다.
>
> 그리고 **증상이 주기적이면 그 주기 자체가 단서다.**
> 랜덤한 실패는 원인 후보가 넓지만, 정확히 60초마다면 "60초로 설정된 무언가"로 범위가 좁혀진다.
