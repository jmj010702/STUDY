## ElasticSearch 개념

### 인덱스
같은 종류의 document를 담는 논리적 공간.

**인덱스 생성 방식**
- 매핑(Mapping) 정의를 통해 역인덱스를 어떻게 만들 것인지 저장
- 분석기(Analyzer) 선택에 따라 역인덱스 구성 방식이 결정됨
- 멀티 필드 정의 지원 — 하나의 매핑에서 여러 분석기 사용 가능

### 샤드
인덱스를 물리적으로 나눈 데이터 분산 저장 단위. 샤드 내부에는 아래 세 가지가 저장됨.

| 구성 요소 | 역할 |
|---|---|
| 역인덱스(Inverted Index) | 검색용 — 어떤 문서에 해당 단어가 있는지 기록 |
| `_source` | 반환용 — 원본 document 데이터 |
| 메타데이터 | 관리용 — `_id`, 버전, 점수 계산 등 ES 내부 정보 |

### Document
JSON 형태의 데이터. 저장 시 인덱스에 정의된 분석기를 거쳐 역인덱스화됨.

- **노리(Nori) 분석기 예시**: 형태소 단위로 분리하여 역인덱스 구성
백엔드  → 문서 1, 5, 9
개발자  → 문서 1, 3, 7


- 역인덱스와 원본 `_source`는 둘 다 샤드에 저장됨

### Field
Document의 각 key 값. 쿼리는 지정된 필드를 기준으로 검색을 수행함.

### CDC (Change Data Capture)
DB에서 발생한 변경 사항을 실시간으로 캡처하는 기술.

- **Debezium**: CDC를 Kafka와 연결하는 커넥터. MySQL의 binlog를 직접 읽어 변경 이벤트를 Kafka 토픽으로 발행함.

---

## AI 기초 개념

| 개념 | 설명 |
|---|---|
| 벡터(Vector) | 텍스트·이미지 등을 숫자 배열로 변환한 것. 의미가 유사할수록 벡터값이 유사함 → 임베딩 |
| BM25 | 키워드 기반 검색 알고리즘 |
| kNN | 유사도(거리) 기반 벡터 검색 알고리즘 |
| RRF (Reciprocal Rank Fusion) | BM25로 상위 N개 문서 추출 + 발화 임베딩 후 kNN 검색, 두 결과를 점수로 합산하여 최종 반환 |
| 벡터 DB | 벡터값을 저장하고 kNN 검색을 제공하는 전용 DB (대표: Pinecone, Weaviate) |
| 임베딩 모델 | 텍스트를 벡터로 변환하는 모델 (대표: OpenAI Embeddings, ES ML 모델) |

### HNSW 알고리즘
벡터 DB의 인덱싱 구조. 계층적 그래프 구조로 유사한 벡터를 빠르게 탐색함. 100% 정확하지는 않지만 근사 최근접 이웃(ANN)을 효율적으로 찾아냄.

---

## AI 처리 흐름

### 발화(utterance)
사용자가 채팅창에 입력한 텍스트 메시지.

### 민감 정보 사전 판별 흐름

페이지 URL 분석 → 민감 URL 여부 판단
↓
발화 내 주민번호 패턴 존재 여부 판단
↓
민감 키워드 포함 여부 판단
↓
하나라도 해당 → 민감 정보 / 모두 미해당 → 안전한 정보



### 안전한 정보일 때 — Claude 처리 흐름

프롬프트 + 도구 + 발화를 HTTP POST로 Claude에 전달
Claude가 메시지를 읽고 사용할 도구를 선택하여 응답 반환
서버에서 해당 도구 실행
도구 실행 결과를 Claude에 전달
Claude가 자연어 답변을 생성하여 반환


### 민감한 정보일 때 — 엑사온(Exaone) 처리 흐름

발화를 HTTP POST로 엑사온에 전달
엑사온이 응답 반환
서버에서 응답 기반으로 후속 처리 실행
처리 결과를 엑사온에 전달
엑사온이 정규식 패턴으로 요청을 처리하여 답변 생성 후 반환


### 프롬프트 캐싱 (Prompt Caching)
첫 실행 시 프롬프트와 색인 도구를 Claude에 전달하여 캐싱. 이후 요청에서 캐시를 재사용해 토큰 소비를 절감하고 응답 효율을 높임.

### 메타데이터 화이트리스트
민감 정보를 Claude에 전달하기 전에 화이트리스트에 등록된 필드만 통과시키고, 미등록 필드는 자동으로 제거함. 이를 통해 민감 정보의 외부 AI 
