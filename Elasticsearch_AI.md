# ElasticSearch
 
**인덱스** : 같은 종류의 document를 담는 논리적 공간
 
**인덱스 생성 방식**
 
- 매핑 정의 → 역인덱스를 어떻게 만들 것인지 저장하는 것
  해당 매핑에서 어떤 분석기를 사용하냐에 따라서 역인덱스를 어떤 방식으로 취할건지 결정됨 → 멀티 필드 정의(한 매핑에서 여러개의 분석기)도 가능
**샤드** : Es에 데이터 분산 저장 단위, 인덱스를 물리적으로 나눈 것 → 실제 저장되는건 역인덱스 테이블, _source(document 원본), 문서 메타데이터(_id, _source 등 es가 관리하는 정보)
 
- 역인덱스 → 검색용(어떤 문서에 있나)
- _source → 반환용(원본 데이터)
- 메타데이터 → 관리용 (문서 Id, 버전, 점수 계산 등)
**document** : json형태의 데이터, 내부적으로는 분석(analyze) 과정을 거쳐 (인덱스에 정의된 상태에 따라서 분류) 역인덱스화를 해 저장. 인덱스에 정의된(매핑) 분석기에 따라 쪼개는 단위가 달라짐 →
 
노리 분석기의 경우 ex) 백엔드 1,5,9 / 개발자 1,3,7 ← 숫자는 문서의 Id 글자는 검색어의 단어
 
- 이런식으로 역인덱스화해 저장을 하지만 원본 document는 따로 저장, 사람에게 반환용 / 사용자에게 해당 문서의 data를 보여줘야 하기 때문에 → 둘 다 샤드에 저장
**field** : document의 각 key값, 쿼리에서 지정된 필드값을 보고 필드에서 찾음.
 
**CDC** : DB에서 발생한 변경 사항을 실시간으로 캡처하는 기술
 
**debezium** : CDC를 카프카와 연결해주는 커넥터, mysql의 binlog를 직접 읽어 변경 이벤트를 kafka 토픽으로 발행
 
# AI 기초
 
**벡터** : 텍스트나 이미지 등 데이터를 숫자 배열로 변환한 것, 의미가 비슷한 단어/문장일수록 벡터값이 비슷함 → 임베딩
 
**BM25** : 키워드 검색
 
**kNN** : 유사도(거리) 기반 검색
 
**RRF** : BM25를 통해 상위 n개 문서를 찾고 발화를에 임베딩 후 kNN 검색으로 유사도가 높은 걸 따로 찾음 그 후에 점수 계산 방식을 통해서 값이 가장 높은 걸 반환
 
**pinecone, weaviate** : 벡터를 저장하고 kNN 검색을 전달하는 특화 DB 서비스 / 즉 벡터 DB
 
**벡터 DB** : 벡터값이 저장되는 DB 여러가지 종류가 있지만 가장 유명한건 pinecone . 기본적으로 kNN 검색 방식을 사용하며 이에 따라 전용 인덱싱 구조(HNSW 알고리즘)가 필요
 
- HNSW : 계층적 구조로 값을 찾아내는 알고리즘. 가장 큰것부터 파고들며 매번 100%정확하진 않지만 가장 유사한 값을 찾아냄
- 
- **임베딩 모델** : 텍스트를 벡터값으로 변환해주는 것, 해당 모델이 없으면 텍스트를 벡터로 변환할 수 없음. 보통 openAi가 많이 쓰이고 그게 아니면 돈을 주고 es에서 지원하는 esML모델 사용
 
# AI 흐름
 
**발화** : 사용자가 채팅창에 입력한 텍스트 메세지
 
페이지 url을 통해서 민감 url인지 아닌지 구분 → 발화에 주민번호 패턴 존재인지 판단 → 민감 키워드 포함 여부 판단 → 여기까지 하나라도 포함된다면 민감정보 아니라면 안전한 정보
 
**안전한 정보일 때 :**
 
프롬프트와 도구를 처음에 HTTP POST 요청으로 발화와 함께 클로드한테 보냄 → 해당 파일과 메세지를 읽고 어떤 도구를 사용할지 분류해서 응답을 내어줌 → 해당 응답을 가지고 서버에서 해당 도구 실행 → 해당 도구를 클로드한테 보냄 → 클로드는 받아서 자연어로 답변 생성 후 반환
 
**민감한 정보일 때 :**
 
해당 질문을 POST 요청으로 엑사온에 전달 → 엑사온 응답 → 응답받은 걸 토대로 다시 서버에서 실행 → 엑사온에 전달 → 엑사온이 정규식 패턴으로 온 요청을 받아서 답변 생성 후 반환
 
**프롬프트 캐싱** → 첫 실행 시 클로드한테 프롬프트 전달 후 색인 도구도 포함 / 이로 인해 토큰이 절감되어 효율적임
 
메타데이터 화이트리스트를 통해서 민감 정보 사전 차단하고 있음. 클로드에게 넘길 때 화이트리스트에 등록된 필드값이 없다면 해당 필드는 제거하고 보냄
 
# 정통 AI와 다른 점이 뭔가
 
**RAG** : 검색으로 보강된 데이터로 텍스트를 생성하는것 . LLM이 답변을 생성할 떄 외부 데이터를 검색해서 함께 넘겨주는 기법 → 키워드 기반 검색(BM25)또는 벡터 검색(kNN)으로 검색
 
위에가 정통 RAG 라면 Agentic RAG라 하여 LLM이 판단을 하여 검색이 필요한지 안한지 먼저 판단하에 답변 생성. 매번 검색을 하지 않아도 된다는 장점이 있음
 
**RAG가 필요한 이유** : LLM은 학습 데이터 기반으로 답변함. 실시간 데이터, 내외부 데이터를 모름.
 
**파인튜닝** : 모델 자체를 변경 → 데이터를 모델 안에 직접 학습함 이로 인해 기존 LLM의 가중치에 추가로 넣고 싶은 데이터를 학습시키면 파인 튜닝된 LLM이 됨.
 
파인튜닝이 유리할 떄는 특정 말투나 형식으로 답변하게 하고 싶을 때, 행동/추론 방식을 변화시키고 싶을 때. 데이터가 잘 변하지 않을 때, 환각을 제거하고 싶을 때, 모델 자체가 너무 커서 비용 문제가 생길 때.
 
**토큰** : LLM이 텍스트를 처리하는 최소 단위. 토큰이 쪼개지는 방식은 각 모델마다 고정되어있음. 보통 영어와 숫자가 좀 더 토큰양이 적음
 
- 토큰은 비용과 직결되기 때문에 ai를 설계할 때 있어서 가장 중요한 부분임
- 프롬프트 캐싱을 통해 프롬프트에 들어가는 토큰을 절감함
- 
- **컨텍스트 윈도우** : LLM이 한번에 볼 수 있는 최대 토큰 수 → 그 전에 나눈 대화나, 프롬프트 등 사전에 정의된 것들 전부 포함해서 최대로 볼 수 있는 양. 토큰이 넘으면 오류가 나거나 앞부분이 일절 짤림
 
**할루시네이션** : 사실이 아닌 것을 자신 있게 답변하는 현상, 틀린 답을 응답해주는 현상
 
- 왜 발생 하는 가 : LLM의 학습 방식은 대량의 텍스트를 학습하고 다음에 올 단어가 뭘지 예측하도록 훈련이 되어있음. 만약 모르는 것이라도 다음 단어를 그럴듯한 걸로 예측함. LLM은 이를 인식하지 못하고 응답을 해버림.
- 막는 방법 : RAG로 실제 문서를 제공해 응답하도록 설계, 시스템 프롬프트로 명시적 금지 등등

-**프롬프트 엔지니어링** : LLM이 원하는 대로 동작하도록 프롬프트를 설계하는 기술
 
few-shot을 사용해 특정 답변 형식을 고정한다던지 CoT 를 사용해 단계별로 생각하게 유도를 한다던지 다양한 방법이 있음
 
- **Temperature** : LLM 출력의 창의성과 일관성을 조절하는 수치 (0~1 또는 0~2)
 
0에 가까울 수록 같은 답변과 창의성이 없어짐 → 정확도가 높음
 
**에이전트** : 스스로 판단해서 도구를 선택하고 실행하는 LLM 시스템, 논리적인 개념임 설계패턴과 같은
 
도구라는 건 구현체도 포함임, 에이전트 안에 RAG를 포함해서 도구로 사용할 수 있음
 
- 에이전트 구현체
  - LLM : 무엇을 할지 판단하는 두뇌
  - Tools : 실행할 수 있는 도구 목록
  - Loop : 목표 달성까지 반복하는 구조
  - Memory : 이전 결과를 다음 판단에 활용
  - 
- **랭체인 (LangChain)** : LLM 기반의 애플리케이션을 쉽게 만들 수 있게 도와주는 프레임 워크
 
랭체인이 없다면 OpenAPi 호출, 벡터 DB연결 코드, RAG 파이프라인 설계, Agent 루프 직접 구현 이런 문제가 생김
 
랭체인을 통해서 어떤 LLM이든 동일한 코드로 호출이 가능하고 RAG 파이프라인 설계, LLM+ Tools + Loop 구조를 템플릿으로 제공함. 추가로 memory까지
 
랭체인을 썼을 때 장단점이 있는데. 장점으로는 빠른 개발이 가능하고 추상화를 제공함. 단점으로는 블랙박스, 버전 변경이 잦음, 커스터마이징 한계, 불필요한 의존성이 있음. 상황에 따라 랭체인을 쓰고 안쓰고 판단을 잘 할 것
 
